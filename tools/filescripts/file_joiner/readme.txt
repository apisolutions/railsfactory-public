there are many situations where you need to know the usage of a code like a css class, a helper method, code duplication finding for refactoring, 


but the files being in different directories or as different files make it difficult to use log processing scripts

then file joiner was born


dumps in root directory all rhtml, all css, all js


usage

ruby fj.rb


modify as needed, add pepper and salt to taste, when used with find.rb/grep (tm) provides best results

 stealable ideas
join all lines of helper methods
join all lines of controllers
join all lines of models


merge a collection of rake task into a new rake file




